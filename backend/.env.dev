# Development overrides (no secrets). Copy settings to `.env` as needed.

# Run ticker/worker inside the API process for convenience
SCHEDULER_IN_APP=1

# Allow dev frontend origin
CORS_ALLOW_ORIGINS=http://localhost:5173

# Surfer mock mode can speed up wiring
SURFER_USE_MOCK=0

